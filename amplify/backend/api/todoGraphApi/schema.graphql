type Todo @model @auth(rules: [{allow: groups, groups: ["Unauthenticated"], operations: [read, create, update, delete]}, {allow: owner}, {allow: private}, {allow: private, provider: iam}, {allow: public, provider: iam}, {allow: public}]) {
  id: ID!
  name: String!
  description: String
  deadline: String
  priority: String
  completed: Boolean!
}
